'use client';
import { FormEvent, useState } from 'react';
import PageShell from '@/components/PageShell';
import { useTable } from '@/lib/crudFactory';
export default function ServicesPage(){const {rows,insert}=useTable('business_services');const [serviceName,setServiceName]=useState('Recruitment');const submit=async(e:FormEvent)=>{e.preventDefault();await insert({service_name:serviceName,status:'Active'});};return <PageShell title='Services Attached' subtitle='What each business is actively consuming.'><div className='card'><form className='row' onSubmit={submit}><select className='select' value={serviceName} onChange={e=>setServiceName(e.target.value)}><option>Recruitment</option><option>ChairPe</option><option>Consulting</option><option>Training</option><option>Buy-Sell</option><option>Academy Partnership</option><option>Influencer Marketing</option><option>Powered-by Salon Shastra</option></select><button className='button'>Attach Service</button></form></div><div className='card'><table className='table'><thead><tr><th>Service</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.service_name}</td><td>{r.status}</td></tr>)}</tbody></table></div></PageShell>}
