'use client';
import { FormEvent, useState } from 'react';
import PageShell from '@/components/PageShell';
import { useTable } from '@/lib/crudFactory';
const types=['Call','WhatsApp','Meeting','Demo','Visit','Proposal','Payment Discussion','Note'];
export default function ActivitiesPage(){const {rows,insert}=useTable('activities');const[type,setType]=useState(types[0]);const[note,setNote]=useState('');const[nextAction,setNextAction]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();await insert({activity_type:type,note,next_action:nextAction});setNote('');setNextAction('');};return <PageShell title='Activities & Follow-ups' subtitle='Log every touchpoint and decide next action.'><div className='card'><form onSubmit={submit} className='grid'><select className='select' value={type} onChange={e=>setType(e.target.value)}>{types.map(t=><option key={t}>{t}</option>)}</select><textarea className='textarea' placeholder='Activity notes' value={note} onChange={e=>setNote(e.target.value)}/><input className='input' placeholder='Next action' value={nextAction} onChange={e=>setNextAction(e.target.value)}/><button className='button'>Save Activity</button></form></div><div className='card'><table className='table'><thead><tr><th>Type</th><th>Note</th><th>Next Action</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.activity_type}</td><td>{r.note}</td><td>{r.next_action}</td></tr>)}</tbody></table></div></PageShell>}
